<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug only: allow the local NestJS server over HTTP on a physical device. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
